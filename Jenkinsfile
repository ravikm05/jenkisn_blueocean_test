pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(poll: true, url: 'https://github.com/ravikm05/jenkisn_blueocean_test.git', branch: 'master', changelog: true)
      }
    }
  }
}